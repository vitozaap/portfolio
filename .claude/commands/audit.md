Your goal is to update any vulnerable dependencies.

Do the following:
1. Run `npm audit` to find vulnerable dependencies in this project.
2. Run `npm audit fix` to apply all updates.
3. Test if the updates didnt break anything.
4. If something break, report to me and undo the update in question. 